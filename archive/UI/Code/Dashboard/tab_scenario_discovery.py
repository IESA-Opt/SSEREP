"""Archived: scenario discovery & premade plots Streamlit page.

This file was moved out of the active dashboard package because the app no
longer exposes this page.

The reusable helper `prepare_results()` was extracted to:
    `UI/Code/Dashboard/utils.py`

If you need the old page implementation for reference, start here.
"""
